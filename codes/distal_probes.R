# get distal probes that are 2kb away from TSS
distal.probes <- get.feature.probe(feature = NULL,
                                   genome = "hg38",
                                   met.platform = "450K")
# 168644 probes
